// deno.json
{
  "tasks": {
    // This task is defined to satisfy the Deno Deploy build step, but does nothing.
    "build": "echo 'Skipping build step...'",
    // This allows local testing if needed.
    "start": "deno run --allow-net --allow-env --allow-read main.ts"
  }
}
